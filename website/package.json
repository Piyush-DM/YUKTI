{
  "name": "yukti-website",
  "version": "1.0.0",
  "private": true,
  "description": "YUKTI public website — static HTML/CSS/vanilla JS. Vite is a dev-server/bundler only; no framework runtime ships (D1).",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "vite": "^5.4.0"
  }
}
